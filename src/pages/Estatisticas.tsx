import { Award, BookOpen, CheckCircle2, Clock3, TrendingUp } from "lucide-react";
import type { AppData } from "../data";
function Estatisticas({ data }: { data: AppData }) {
  const completed = data.tasks.filter((task) => task.completed).length; const mastered = data.flashcards.filter((card) => card.mastered).length; const chart = [42, 65, 38, 82, 60, 91, 74];
  return <main className="page"><section className="stats-grid four"><article className="stat-card"><span className="stat-icon blue"><Clock3 /></span><div><small>Tempo focado</small><strong>{Math.floor(data.focusMinutes / 60)}h {data.focusMinutes % 60}min</strong></div></article><article className="stat-card"><span className="stat-icon green"><CheckCircle2 /></span><div><small>Tarefas concluídas</small><strong>{completed}</strong></div></article><article className="stat-card"><span className="stat-icon orange"><BookOpen /></span><div><small>Cartões dominados</small><strong>{mastered}</strong></div></article><article className="stat-card"><span className="stat-icon purple"><Award /></span><div><small>Sequência</small><strong>7 dias</strong></div></article></section><section className="dashboard-grid stats-layout"><article className="panel chart-panel"><div className="panel-heading"><div><span className="eyebrow">ÚLTIMOS 7 DIAS</span><h3>Ritmo de estudos</h3></div><span className="trend"><TrendingUp size={16} /> +18%</span></div><div className="bar-chart">{chart.map((value, index) => <div key={index}><span style={{ height: `${value}%` }} /><small>{["Seg", "Ter", "Qua", "Qui", "Sex", "Sáb", "Dom"][index]}</small></div>)}</div></article><article className="panel subject-panel"><div className="panel-heading"><div><span className="eyebrow">POR MATÉRIA</span><h3>Distribuição</h3></div></div>{[["Física", 78], ["Cálculo", 62], ["Literatura", 44]].map(([name, value]) => <div className="subject-progress" key={name}><div><strong>{name}</strong><span>{value}%</span></div><progress value={value} max="100" /></div>)}</article></section></main>;
}
export default Estatisticas;
